# 执行进度

## 项目信息

- 项目：SVN 图形化管理工具（TortoiseSVN 类桌面应用，macOS 优先）
- 技术栈：Tauri 2 + Rust + Vue 3 + TypeScript + Vite
- SVN 接入：封装 SVN CLI（XML 输出、参数数组、`LC_ALL=C`、原始字节、禁止 shell 拼接）
- 计划来源：《SVN图形化管理工具开发计划.md》（V1.0，含 2026-08-03 修订）
- 执行启动：2026-08-03

## 里程碑总览

| 里程碑 | 目标 | 状态 |
|---|---|---|
| M0 环境核实 | svn/cargo/rustc/node/npm 可用 | ✅ 完成 |
| M1 骨架 | Tauri 窗口 + IPC + svn 调用可用 | ✅ 完成 |
| M2 核心层 | Command Runner / XML Parser / 错误分类 / 写操作串行 | ✅ 完成 |
| M3 MVP 闭环 | 远程浏览、checkout、status、Diff、update、commit、日志 | ✅ 完成 |
| M4 测试通过 | cargo test 全绿 + 前端 typecheck/build 通过 + GUI 启动不崩溃 | ✅ 完成 |

## 环境核实记录

| 组件 | 版本 | 路径 |
|---|---|---|
| svn | 1.14.5 | /opt/homebrew/bin/svn |
| svnadmin | 1.14.5 | /opt/homebrew/bin/svnadmin（测试建仓用） |
| cargo / rustc | 1.97.1 | /opt/homebrew/bin |
| node / npm | v22.22.3 / 10.9.8 | $HOME/.hermes/node/bin |
| Xcode | CommandLineTools 6.3.2 | /Library/Developer/CommandLineTools |
| @tauri-apps/cli | ^2.11.4（项目依赖） | node_modules |

## 项目结构

```
.
├── 执行进度.md / 问题记录.md / SVN图形化管理工具开发计划.md
├── manage.sh                  # 管理脚本：start/stop/status/logs/test/check/build（.run/ 存 PID 与日志）
├── package.json               # Vite 1421 端口（与 Git 工具 1420 错开）
├── index.html / vite.config.ts / tsconfig.json
├── assets/icon.png            # SVN 风格图标源图（scripts/make_icon.py 生成）
├── scripts/
│   ├── make_icon.py           # 图标生成脚本（纯标准库手写 PNG）
│   └── check_icon.py          # 图标校验脚本（尺寸/主色/对比/预览）
├── src/                       # Vue 3 前端
│   ├── main.ts / App.vue / style.css
│   ├── types.ts               # 与 Rust 模型对应类型 + 错误规范化
│   ├── api.ts                 # invoke 封装
│   └── components/
│       ├── RemoteView.vue     # 远程浏览：目录/预览/日志/diff
│       └── WcView.vue         # 工作副本：状态/差异/提交/更新/add/revert
└── src-tauri/
    ├── Cargo.toml             # tauri 2 / quick-xml / base64
    ├── tauri.conf.json        # SvnDesktop，1280x820
    ├── capabilities/default.json
    ├── src/
    │   ├── main.rs / lib.rs   # 命令注册（14 个）
    │   └── svn/
    │       ├── runner.rs      # svn 发现 + 参数数组 + LC_ALL=C + utf8_escape
    │       ├── models.rs      # 数据模型 + SvnError 错误码分类
    │       ├── parser.rs      # quick-xml 解析 info/list/log/status
    │       ├── commands.rs    # 14 个 Tauri 命令（写操作串行锁）
    │       └── task.rs        # 全局写锁
    └── tests/svn_flow.rs      # 真实仓库闭环集成测试（2 个）
```

## 已实现功能（MVP 0.1.0）

远程仓库页：
- URL 打开（svn info --xml 预检 + 目录条目预检），显示根/UUID/HEAD revision/最近提交
- 目录浏览：单层 list 惰性加载 + 面包屑导航（虚拟列表待 P1 大目录优化）
- 文件预览：`svn cat` 原始字节 → base64 → 前端 UTF-8/GBK 解码；二进制 NUL 判定
- 日志：`svn log --xml -v` 最近 50 条，含变更路径
- 变更：点日志行 → `svn diff` 该 revision 相对上一 revision 的差异（peg 回退逻辑）

工作副本页：
- 打开目录：info + status 合并展示（根/URL/revision）
- 状态列表：modified/add/delete/unversioned/conflicted 等，勾选提交
- 本地 Diff：点行查看（svn diff）
- 提交：`-F` 0600 临时文件 + `--encoding utf-8`（中文消息）；串行锁
- 更新 / 添加（svn add --parents）/ 还原（svn revert，confirm 确认）

安全与健壮性：
- 全部子进程参数数组启动，禁止 shell 拼接；`LC_ALL=C` 稳定输出
- 错误按 svn 错误码分类（E/W 前缀均识别），禁止泛子串匹配
- 写操作全局串行锁；commit 消息走 0600 临时文件
- 非 UTF-8 字节 utf8_escape 兜底；XML 解析失败保留原始输出

## 验证记录

| 项目 | 结果 |
|---|---|
| cargo test（13 单元 + 2 集成） | ✅ 15 passed（2026-08-03） |
| 集成测试 full_mvp_flow | ✅ 真实仓库完整闭环：open→list→cat(中文/二进制)→log→checkout→modify→status→diff→add→commit(中文消息)→远程日志确认→远程diff→update→revert |
| 集成测试 error_classification | ✅ not-found / not-working-copy 真实分类 |
| npm run typecheck（vue-tsc） | ✅ 通过 |
| npm run build（vite） | ✅ 76 KB JS |
| GUI 冒烟（tauri dev） | ✅ vite 1421 返回 200，进程存活；WebKit WebsiteData 警告为 dev 模式已知问题 |
| cargo test（19 单元 + 2 集成） | ✅ 21 passed（2026-08-04，含 diff_chunks 6 用例） |
| npm run build（vite） | ✅ 332 KB JS（含 @codemirror/merge） |
| GUI 冒烟（diff 并排） | ✅ 窗口进程存活无崩溃；并排交互效果待人工确认 |
| cargo test（19 单元 + 4 集成） | ✅ 23 passed（2026-08-04，含 delete_and_commit_flow 删除闭环） |
| cargo test（19 单元 + 5 集成） | ✅ 24 passed（2026-08-04，新增 resolve_cleanup_upgrade_flow 冲突解决闭环） |
| cargo test（19 单元 + 6 集成） | ✅ 25 passed（2026-08-04，新增 remote_write_flow 远程写闭环） |
| GUI 冒烟（resolve/cleanup/upgrade） | ✅ 窗口进程存活，前端 1421 正常 |
| GUI 冒烟（远程写） | ✅ 窗口进程存活，前端 1421 正常 |
| npm run build（vite） | ✅ 334.85 kB JS |
| GUI 冒烟（检出/删除） | ✅ 窗口进程存活，前端 1421 正常 |

## 进度日志

### 2026-08-03 ｜ 第一阶段（MVP 0.1.0）完成

- [x] 创建执行进度文档与问题记录文档
- [x] M0 环境核实（全部工具链就绪）
- [x] M1 项目骨架（Tauri 2 + Vue 3 + TS + Vite，端口 1421）
- [x] M2 核心层（runner / models / parser / task）
- [x] M3 命令层（14 个命令，写操作串行）
- [x] M4 前端（远程浏览 + 工作副本两页）
- [x] M5 验证（cargo test 全绿 + typecheck/build + GUI 冒烟）
- [x] manage.sh（dev/test/check/build）

### 2026-08-04 ｜ 第二阶段（diff 并排视图 + 参考项目借鉴）

- [x] 调研开源 SVN GUI：OrcaSVN（Tauri+Rust+Vue，栈一致）clone 至 /tmp/OrcaSVN 只读参考，不引入代码
- [x] 借鉴 1：svn 二进制可配置（runner RwLock + set_svn_executable 命令 + 前端设置弹窗 ⚙）
- [x] 借鉴 2：svn 子进程 120s 超时自动 kill（双线程 drain 管道防阻塞，对齐“只读任务可取消”语义）
- [x] 借鉴 3：未版本化文件 diff 语义（无基线=全新增，DiffResult.unversioned + 并排视图左侧留空）
- [x] DiffView.vue：@codemirror/merge 双栏并排（行内高亮、折叠/展开未变更块、双向同步滚动、差异统计 +N/−M/处数）
- [x] 本地 diff 接入：wc_file_pair（svn cat -r BASE 基线 vs 当前文件）→ DiffView
- [x] 远程日志 diff 接入：remote_cat×2（旧版本失败降级提示）→ DiffView（标题 r旧→r新）
- [x] 大文件分流：>400K 字符时 diff 计算下沉 Rust（similar crate + diff_chunks IPC + Chunk 构造），主线程零卡顿
- [x] manage.sh（start/stop/status/logs/test/check/build）

### 2026-08-04 ｜ 第三阶段（远程检出 + 工作副本删除）

- [x] 远程页文件列表行新增“检出”按钮：选本地目录 → svn checkout 到 目录/名称 → 自动切到“工作副本”页并打开（App 传 wcPath，WcView initialPath 自动 open）
- [x] 后端 wc_delete（svn delete --force，写锁）：本地标记删除，提交后服务端生效；空路径校验
- [x] 工作副本页新增“删除选中”按钮（确认框说明“本地标记删除，提交后生效”），执行后自动刷新状态列表
- [x] 删除闭环集成测试：删文件+删目录 → status 显示 deleted → commit → 远程 cat 确认 not-found

### 2026-08-04 ｜ 第五阶段（功能待办批次 2：远程写操作）

- [x] 后端 5 命令：remote_mkdir / remote_delete / remote_copy（分支标签）/ remote_move / remote_import（均写锁，提交说明 -F 临时文件 + --encoding utf-8 防中文 E000022）
- [x] 远程页新增 writebar：提交说明输入 + 新建目录 / 创建分支标签 / 导入目录按钮
- [x] 列表行新增“重命名”“删除”（强确认，删除立即提交不可撤销）
- [x] doWrite 共用流程：执行 → 提示 → 清空消息 → 刷新目录与日志
- [x] 集成测试 remote_write_flow：中文目录 mkdir → copy 分支（内容一致）→ move → import（含子目录）→ delete 全闭环
- [ ] 待推进：批次 3 属性与锁定（proplist/propset svn:ignore / lock / unlock）

### 2026-08-04 ｜ 第七阶段（功能待办批次 4：本地编辑与日志）

- [x] `svn move` 本地移动/重命名（保留历史），WcView「移动/重命名」按钮（选中 1 项 → 输入新名或目标路径）
- [x] `svn propset --revprop -r REV svn:log -F 临时文件` 编辑提交说明（需服务端 pre-revprop-change hook；hook 未配置时提示错误）
- [x] `remote_log` 过滤参数：`--search`（匹配说明/作者/路径/日期）+ `-r {日期}:{日期}` 范围（纯日期 to 侧自动补 T23:59:59）
- [x] RemoteView 日志过滤区（搜索框 + 起始/截止日期 + 过滤/清除）+ 每条日志「编辑说明」按钮
- [x] 验证：`cargo test` 27 项全绿（19 单元 + 8 集成，新增 local_move_and_log_flow）、typecheck、build、GUI 冒烟
- [ ] 注意：本机 svn 1.14.5 无 `--author` 选项，作者过滤由 `--search` 覆盖

### 2026-08-04 ｜ 第八阶段（功能待办批次 5：浏览辅助）

- [x] `wc_blame`（`svn blame` 纯文本解析，兼容 `r` 前缀与定宽空格对齐；`blame --xml` 在此 svn 版本不输出行文本，故走纯文本）
- [x] `wc_status_u`（`svn status -u --xml`）：`parse_status` 增加 `<against>` 解析与 `<repos-status>` 字段，状态行显示 ↻ 服务器端状态 + 服务器最新 revision
- [x] 工作副本自动刷新：间隔 15s / 30s / 60s 下拉选择，切换即生效，卸载时清理定时器
- [x] changelist：`wc_changelist`（加入/移除）+ `wc_commit_cl`（`svn commit --changelist`，须以工作副本根为 cwd）
- [x] WcView：每行「Blame」按钮 + 弹层（行号/版本/作者/内容）；操作栏「与服务器比较 / 加入变更集 / 移除变更集 / 提交变更集」
- [x] 验证：`cargo test` 28 项全绿（19 单元 + 9 集成，新增 browse_aux_flow）、typecheck、build、GUI 冒烟

### 2026-08-04 ｜ 第九阶段（功能待办批次 6：P2 低优先功能项）

- [x] `wc_switch`（`svn switch [--depth D] URL PATH`）：切换分支/URL + 稀疏目录深度（empty/files/immediates/infinity）
- [x] `wc_merge`（`svn merge [-r F:T] SOURCE TARGET`）：支持显式版本范围与 mergeinfo 自动合并，`dry_run` 只读不加写锁（前端保留后续接入）
- [x] `wc_relocate`（`svn relocate FROM NEW PATH`）：本机 svn 1.14 只支持位置参数语法（无 `--from/--to`，实测修正）
- [x] 补丁：`wc_diff_text`（`svn diff` 返回文本）+ `wc_patch_apply`（补丁写临时文件 0600 → `svn patch`）
- [x] WcView 操作栏：「切换分支/URL」「重定位」「合并」「创建补丁」「应用补丁」；补丁弹层（创建=只读展示+复制，应用=粘贴+应用）
- [x] 集成测试 batch6_switch_merge_relocate_patch：建分支（`svn copy --parents`，父目录不存在会 E160013）→ switch 往返 → 分支修改合并回 trunk → 补丁创建/revert/应用 → 仓库整体复制后 relocate + update 可用
- [x] 内置三方合并编辑器暂缓（工程量大，冲突时走编辑文件 + resolve）；长任务异步化 / 大目录虚拟列表列入体验缺口持续跟进
- [x] 验证：`cargo test` 29 项全绿（19 单元 + 10 集成）、typecheck、build、GUI 冒烟

### 2026-08-04 ｜ 第十阶段（功能待办批次 7：大目录虚拟列表）

- [x] 新增 `VirtualList.vue` 通用虚拟滚动组件：固定行高 + scrollTop 计算可视窗口（±buffer），spacer 撑高 + translateY 窗口定位，数据源变化自动回顶；`generic="T"` 泛型使插槽 item 类型推导（目录传 ListEntry、日志传 LogEntry）
- [x] RemoteView 目录列表由 `<table>` 改为网格（.listhead 表头 + .vrow 行，grid-template-columns 对齐），接入 VirtualList，行高 30px
- [x] 日志列表由 `<ul>/<li>` 改为 VirtualList + .vlogrow（复用原 li 样式），pickLog 仍传 index
- [x] 验证：typecheck 干净、build 成功、GUI 冒烟（1421 → 200 → stop 无残留）；构建 5000 文件单层仓库（svn list 返回 5000 条）作大目录数据源，滚动流畅度待人工确认
- [ ] 后续：RemoteView 列表行高与操作列宽度如需微调，可在真机大目录上验证后调整

## 本阶段发现并修复的问题

### 2026-08-04 ｜ 第四阶段（功能待办批次 1：冲突解决 / 清理 / 升级）

- [x] 新建 功能待办.md：按 6 批次收录全部未实现功能清单（对照开发计划 §5.5/§6/§6.5），实现后逐项勾选
- [x] 后端 wc_resolve（svn resolve --accept，策略由前端选择）、wc_cleanup、wc_upgrade，均走写锁
- [x] 工作副本页操作区：勾选 conflicted 路径时出现「解决冲突」按钮 + 策略下拉（working/mine-full/theirs-full/base）；新增「清理」「升级」按钮
- [x] 集成测试：双工作副本改同文件 → update 冲突 → status conflicted → resolve theirs-full → 冲突清除且内容正确 → cleanup/upgrade no-op 成功
- [ ] 待推进：批次 2 远程写操作（mkdir / delete / copy / move / import）

### 2026-08-04 ｜ 第六阶段（功能待办批次 3：属性与锁定）

- [x] `svn proplist -v --xml` 解析（工作副本与远程 URL 通用）
- [x] `svn propset NAME -F 临时文件`（多行 svn:ignore 值）+ propdel
- [x] `svn lock -F 注释` / `svn unlock [--force]`
- [x] 状态列表解析 `<lock>` 块（owner / comment），锁定文件行显示 🔒
- [x] WcView 属性弹层（查看 / 新增 / 更新 / 删除属性）+ 锁定 / 解锁 / 强制解锁按钮
- [x] 验证：`cargo test` 26 项全绿（19 单元 + 7 集成，新增 props_lock_flow）、typecheck、build、GUI 冒烟

共 8 项，全部登记于《问题记录.md》，7 项已解决、1 项（WebKit 沙盒警告）已注明处理路径。

### 2026-08-04 ｜ 第十一阶段（功能待办批次 8：长任务异步化 TaskManager）

- [x] `runner.rs` 重构：`run_svn_inner` 支持 `Option<&AtomicBool>` 取消标志，50ms 轮询检测到取消即 `kill` 子进程；新增 `run_svn_cancellable`；原 `run_svn_any_with_bin` / `run_svn_any` 转调内部函数（行为不变）
- [x] `task.rs` 重写为 TaskManager：全局任务表（id/desc/状态 running-done-failed-cancelled/起止时间/输出/结果摘要），`Arc<AtomicBool>` 取消标志，历史保留最近 50 条；保留写串行锁 `write_lock`；单元测试 `task_lifecycle_works` / `task_list_keeps_history_and_trims`
- [x] `commands.rs` 新增：`task_checkout` / `task_update` / `task_import` / `task_export`（后台线程 + 写锁 + 可取消）+ `task_list` / `task_cancel`；通用 `spawn_svn_task`（`Send + 'static` 闭包生成 args）；`task_summary` 取最后非空行作结果摘要；`task_import` 的 `-F` 临时消息文件在后台线程创建/清理
- [x] `lib.rs` 注册 6 个新命令
- [x] 前端：`types.ts` 加 `TaskInfo`/`TaskState`；`api.ts` 加 6 个任务 API；新建 `TaskBar.vue`（1s 轮询 task_list，进行中旋转图标 + 取消，失败详情弹层，保留最近 5 条）；`App.vue` 全局挂载；RemoteView 导出/检出/导入/下载、WcView 更新改走 `task_*`，UI 全程不卡
- [x] 集成测试 `task_flow`：后台 checkout 完成（Done + .svn 产出 + 结果摘要）、已结束任务不可取消（false）、不存在的 URL 失败（Failed + 错误输出）、3000 文件大目录导出立即取消（Cancelled）
- [x] 验证：`cargo test` 32 项全绿（21 单元 + 11 集成）、typecheck、build（158ms）、GUI 冒烟（1421 → 200 → stop 无残留）

### 待办（剩余未实现项）
- 证书永久信任机制（三候选：servers:ssl-authority-files 配置 CA / 命令行一次性接受写入缓存 / 隔离 HOME；批次 15 已实现「临时接受」，永久信任待 HTTPS fixture 验证后定案）
- P1：提交对话框内嵌 diff 预览 + 按文件部分提交
- P1：文件 watch 增量刷新（替代 15/30/60s 轮询）
- P2：日志高亮当前 WC 版本 + 一键复制 revision
- P2：mergeinfo 查看（已合入/未合入 revision 图形化）
- P2：checkout/export 目录选择对话框 + 深度/版本选项
- P2：CI 发布流水线（DMG 构建 + GitHub Release）
- P3：Finder 右键集成（用本工具打开）
- P3：revision graph 日志图（分支演化可视化）

### 2026-08-06 ｜ 第十二阶段（功能待办批次 10：访问历史下拉）

- [x] 后端新建 `history.rs`：`history_add(kind, value)` / `history_list()`，JSON 持久化 `$HOME/.config/svn-desktop-tool/history.json`（独立文件避开 dev 模式 WebKit localStorage 沙盒问题）；同 kind + value 去重（新记录移到最前）、按最近使用排序、上限 50 条；`Mutex` 串行化文件读写；3 个单元测试（去重移前 / 截断保留最新 / 缺文件不 panic）；`mod.rs` 挂载、`lib.rs` 注册
- [x] 前端：`types.ts` 加 `HistoryEntry`；`api.ts` 加 `historyList` / `historyAdd`；RemoteView 仓库 URL 输入框与 WcView 工作副本路径输入框均加 `<datalist>` 下拉（按 kind 过滤展示），组件挂载时拉取历史，**打开成功后才记录**（失败地址不污染历史）
- [x] 修复历史遗留测试 bug：`local_move_and_log_flow` 日期过滤硬编码 2026-08-04，改为动态取系统日期（`date +%Y-%m-%d`），避免跨天失效
- [x] 验证：`cargo test` 35 项全绿（24 单元含 3 个 history 测试 + 11 集成）、typecheck 干净、build 成功（355.32 kB）、GUI 冒烟（1421 → 200 → stop 无残留）

### 2026-08-06 ｜ 第十三阶段（功能待办批次 9：内置三方合并编辑器）

- [x] 实测确认 svn 1.14.5 冲突标记格式：`<<<<<<< .mine` / `||||||| .r3`（base 共同祖先，后缀 revision）/ `=======` / `>>>>>>> .r4`；方向为 mine=本地、theirs=远端；冲突生成 `f.txt.mine/.rN` 备份文件
- [x] `parser.rs` 新增 `parse_conflict_bytes` 状态机 + `assemble_conflict` 组装：字节级解析（非 UTF-8 不损坏）、行尾检测（CRLF/LF）还原、`=======` 仅在块内识别（markdown 分隔线不误判）、未闭合块报错、尾部空行处理；`ConflictPart::Context/Block` 保留块间上下文；9 个单元测试（方向/无 base/多块/误判防御/未闭合/CRLF/4 选择矩阵/上下文保留/非 UTF-8）
- [x] `models.rs` 加 `ConflictBlock` / `ConflictInfo`；`commands.rs` 新增 `wc_conflict_parse`（读文件→解析→三栏文本）与 `wc_conflict_resolve`（写锁→重读重解析→块数校验→按选择组装写回→`svn resolve --accept working`），无标记/数量不匹配/非法选择均有明确错误；`lib.rs` 注册
- [x] 前端：`types.ts` 加 `ConflictBlock`/`ConflictInfo`/`ConflictChoice`；`api.ts` 加两个 API；新建 `MergeDialog.vue`（上/下一处块导航、本地/基准/远端三栏、取本地/取远端/都留/都删 4 选、当前块实时预览、全部处理后才可「保存并解决」、取消不写回）；WcView conflicted 行加「三方合并」按钮，关闭后自动刷新
- [x] 集成测试 `merge3_flow`：双 wc 改同一行 → 冲突 → 方向断言 → 4 种选择场景矩阵（含备份文件清理断言）→ 多块逐块选择 → 数量不匹配报错 → 无标记文件 → 提交闭环（远程内容 = 合并结果）；发现并规避 svn 行为：**conflicted 状态文件再次 update 会被 svn 跳过**，须先 resolve
- [x] 验证：`cargo test` 46 项全绿（34 单元 + 12 集成）、typecheck 干净、build 成功（359.78 kB）、GUI 冒烟（1421 → 200 → stop 无残留）

### 2026-08-06 ｜ 第十四阶段（功能待办批次 11/12/13：认证设置页 · diff 增强 · 冲突引导）

- [x] 批次 11 认证设置页：
  - `parser.rs` 新增 `parse_auth_creds`（分隔线分块、英文键名、svn.simple/svn.ssl.server）+ `unescape_u_escapes`（还原 svn `{U+XXXX}` 转义，变长十六进制）+ 3 单测；`models.rs` 加 `AuthCred`
  - `runner.rs` 支持 `stdin_data`（写后关闭管道）+ 新增 `run_svn_auth`（`--non-interactive --username U --password-from-stdin`，密码仅进 stdin，不进参数/日志/URL）
  - `commands.rs` 新增 `svn_auth_list` / `svn_auth_remove`（"*" 全清，E200009 无匹配视为已空）/ `remote_open_auth`（认证重试）；`lib.rs` 注册
  - 前端：设置弹层加「认证缓存」区块（凭据列表/单条清除/全部清除/刷新，打开设置自动加载）；RemoteView 连接认证失败（category=auth）自动弹用户名密码重试弹窗
  - 集成测试 `auth_flow`：svnserve -d --pid-file + 隔离 HOME（预建 .subversion 才写缓存）：无凭据 E170001→auth、正确密码 stdin 成功、错误密码失败；实测确认 `svn auth --remove "<realm>"` 精确删除有效
  - 证书信任三候选机制标注「待 HTTPS fixture 验证后定案」待办
- [x] 批次 12 并排 diff 增强：`npm install @codemirror/language-data`；DiffView 语法高亮（扩展名/filename 匹配 + 异步 load 后重建 MergeView 应用 a/b.extensions）、「上一处/下一处」跳转（mv.b + EditorView.scrollIntoView + N/M 标签）、「外部对比」按钮 + 双击（后端 `wc_diff_external` 导出 BASE/工作区临时文件 + `opendiff` spawn）；WcView 传 :path 启用
- [x] 批次 13 冲突处理引导：`doUpdate` 轮询任务完成（≤120s）→ refresh → 检测 conflicted 自动弹引导条（冲突路径清单 + 每行「三方合并」直达 + 策略解决提示）；`conflictCount` 存在冲突时禁用提交（canCommit 条件 + commit() 守卫 + 工具栏 ⚠ 红字）
- [x] 验证：`cargo test` 50 项全绿（37 单元含 3 认证解析测试 + 13 集成含 auth_flow）、typecheck 干净、build 成功（417.72 kB）、GUI 冒烟（1421 → 200 → stop 无残留）

### 2026-08-06 ｜ 体验修正（远程目录名列宽 · 导入语义 · 状态恢复）

- [x] 修复非最大化窗口看不到远程目录名：目录列表 grid 固定列合计 552px 把 `1fr` 名称列压到 0；改为名称列 `minmax(120px,1fr)` 保底 + 压缩次要列 + 操作列图标化（⤓⤵✎🗑，116px）+ `@media (max-width:1250px)` 隐藏类型/大小/revision/作者保住名称列 + `.c-name .link` min-width:0 防长名撑破
- [x] TaskBar 进行中任务显示耗时（进行中… 12s / 1m05s）；导入任务完成后自动刷新目录/日志并在 notice 显示「导入完成：Committed revision N」
- [x] 修复刷新/切页后远程页空白：RemoteView 挂载时从 history 自动恢复最近访问的远程仓库（history.json 已有 https://svn.example.internal/…记录）
- [x] 导入目录语义修正：`svn import` 导入的是目录"内容"（目录名不出现）→ 目标 URL 改为 `currentUrl() + "/" + 本地目录名`，远程仓库出现同名子目录；导入前弹确认窗显示目标位置；同名已存在走方案 A（svn 自身报错/合并，错误原样显示）

### 2026-08-06 ｜ 第十五阶段（功能待办批次 14：远程仓库收藏）

- [x] 后端新建 `favorites.rs`：`fav_add(name, url)` / `fav_list()` / `fav_remove(url)`，JSON 持久化 `$HOME/.config/svn-desktop-tool/favorites.json`；按 URL 去重（同 URL 更新名称并移到最前）、上限 100、空名称/URL 校验；`Mutex` 串行化文件读写；3 个单元测试；`mod.rs` 挂载、`lib.rs` 注册
- [x] 前端：`types.ts` 加 `Favorite`；`api.ts` 加 `favAdd`/`favList`/`favRemove`；RemoteView 顶部 openbar 加「☆ 收藏 / ★ 已收藏」按钮（收藏当前浏览目录，默认名取 URL 最后一段可改，已收藏时点击取消）；openbar 下加收藏夹 chip 栏（点击 chip 切换过去、× 取消收藏、当前项高亮）；onMounted 加载收藏
- [x] 验证：`cargo test` 53 项全绿（40 单元含 3 个收藏测试 + 13 集成）、typecheck 干净、build 成功、GUI 冒烟（1421 → 200 → stop 无残留）

### 2026-08-06 ｜ 体验修正（导入超时与体检）

- [x] 修复导入 120 秒超时问题（344MB 大目录导入被 kill）：`runner.rs` 新增 `SVN_TIMEOUT_LONG`(30 分钟)，`run_svn_inner` 增加 timeout 参数，新增 `run_svn_cancellable_long`；4 个批量任务（task_import/checkout/update/export）改走长超时，只读命令保持 120s 防卡死
- [x] 新增 `dir_stats(path)` 命令（`#[tauri::command]` 包装 + `dir_stats_inner` 纯函数）：递归统计文件数/总大小/大文件(>5MB)列表/垃圾文件(.DS_Store/*.pyc/*.err/*.tmp/*.swp/*~)列表 + `dir_stats_works` 单测
- [x] 前端导入确认弹窗集成体检：选目录后显示「文件数 / 总大小」+ 大文件上传耗时警告 + 垃圾文件清理建议；统计失败时提示"将直接导入"
- [x] 排查中发现 commands.rs 存在两个测试模块（`mod tests` 与 `mod diff_chunks_tests`，后者仅导入 diff_chunks/line_offsets），新增测试须加入对应模块的 use
- [x] 验证：`cargo test` 54 项全绿（41 单元含 dir_stats 测试 + 13 集成）、typecheck、build、GUI 冒烟

### 2026-08-06 ｜ 体验修正（路径合并 · window.prompt 全面替换）

- [x] 远程页路径合并：面包屑（crumb）从 left pane 移到 infobar 下方横跨全宽，成为唯一路径导航（⤴ 根悬停 title 显示完整 rootUrl）；infobar 精简为 revision / UUID / 最近提交 / notice，删除重复的「根：URL」「相对路径」展示
- [x] 根因：macOS Tauri 2（WKWebView）不桥接 `window.prompt`（返回 null 且无弹窗）→ 收藏按钮、新建目录、创建分支、重命名等点按钮无反应；新建 `PromptDialog.vue` 通用输入弹窗 + `src/prompt.ts` 的 `usePrompt()`（`uiPrompt(title, initial, message)` 返回 Promise，支持 Enter/Esc/默认值/自动聚焦全选）
- [x] 替换全部 18 处 `window.prompt`：RemoteView 6 处（收藏命名/mkdir/copy/rename/日志编辑说明）+ WcView 12 处（锁定注释/移动重命名/变更集名与说明/switch URL 与 depth/relocate 新地址与旧前缀/merge 源与范围）；相应函数改 async
- [x] 验证：`cargo test` 54 项全绿（41 单元 + 13 集成）、typecheck、build、GUI 冒烟

### 2026-08-06 ｜ 第十六阶段（P0/P1 优化：认证重试全局化 · 任务重试 · 收藏管理）

- [x] P0-1 认证重试全局化：`authGuard` 通用包装（捕获 E170001 → `promptAuth` 弹认证窗 → `remoteOpenAuth` 写入凭据缓存 → pendingOp 自动重试原操作）；覆盖浏览目录 `loadDir` / 日志 `loadLogs` / 文件 `showFile` / 写操作 `doWrite` / diff 两处 `remoteCat`；`open()` 认证失败改为弹窗后重新 open；取消走 `cancelAuth`；浏览途中 token 过期不再需要重新打开仓库
- [x] P0-2 任务重试：`task.rs` 加 `RetrySpec`（Import/Checkout/Update/Export 原始参数）+ `create_task_with_retry` + `get_retry`（仅 Failed/Cancelled）+ 单测；`commands.rs` 4 个 task 命令存参数，新增 `task_retry(id)`（取参数重建新任务，`-F` 消息文件重试时重建）；TaskBar 失败/取消项加「重试」按钮
- [x] P1-4 收藏管理：`favorites.rs` 加 `fav_clear`；设置弹层加「收藏夹」区块（列表/单条删除/全部清除/刷新），打开设置自动加载
- [x] 验证：`cargo test` 55 项全绿（42 单元 + 13 集成）、typecheck、build、GUI 冒烟

### 2026-08-06 ｜ 第十六阶段（P1 优化：分支管理 · 证书信任 · svn:ignore 批量 · 外部 diff 配置）

- [x] 分支/标签管理：后端 `remote_repo_layout`（探测 root/trunk、root/branches、root/tags + 列出分支/标签，容错不报错）；前端打开仓库后自动探测，面包屑旁加「分支」「标签」下拉（选择即切换）+「+ 创建分支/标签」（confirm 选类型 + uiPrompt 名称 → svn copy trunk→root/branches|tags/名称）
- [x] 证书信任：错误分类 certificate（已有）→ 连接失败弹窗「是否临时接受证书」→ `remote_open_trust`（`--trust-server-cert-failures=unknown-ca,cn-mismatch,expired,not-yet-valid,other` 重连，仅本次不写缓存）
- [x] svn:ignore 批量：后端 `wc_ignore_add(paths)`（按父目录分组 → 读现有 svn:ignore → 合并缺失项 → propset -F 写回）；前端「添加选中」旁加「加入忽略」按钮（选中 unversioned 时显示）
- [x] 外部 diff 工具可配置：新建 `settings.rs`（JSON 持久化，默认 /usr/bin/opendiff）；`wc_diff_external` 改用配置工具路径（参数：基线文件 工作副本文件）；设置页加「外部 diff 工具」输入框 + 保存
- [x] 验证：`cargo test` 55 项全绿（42 单元 + 13 集成）、typecheck、build、GUI 冒烟
