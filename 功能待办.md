# 功能待办清单

> 记录尚未实现的功能，分批次推进；完成一项即把状态改为 ✅ 已实现（附日期）。
> 来源：对照《SVN图形化管理工具开发计划.md》§2.3/§5.5/§6/§6.5 与日常使用频率梳理（2026-08-04）。
> 关联：执行进度.md（实现过程记录）、问题记录.md（问题登记）。

## 批次总览

| 批次 | 内容 | 状态 |
|---|---|---|
| 批次 1 | 工作副本维护：resolve / cleanup / upgrade | ✅ 已实现（2026-08-04） |
| 批次 2 | 远程写操作：mkdir / delete / copy（分支标签）/ move / import | ✅ 已实现（2026-08-04） |
| 批次 3 | 属性与锁定：proplist / propset（svn:ignore）/ lock / unlock | ✅ 已实现（2026-08-04） |
| 批次 4 | 本地 move / 编辑提交说明 revprop / 日志过滤 UI | ✅ 已实现（2026-08-04） |
| 批次 5 | 浏览辅助：blame / status -u / 自动刷新 / changelist | ✅ 已实现（2026-08-04） |
| 批次 6 | P2 低优先：switch / merge / relocate / 稀疏目录 / 补丁 / 三方合并 | ✅ 已实现（功能项，2026-08-04） |
| 批次 7 | 大目录虚拟列表：VirtualList 组件 / 目录列表 / 日志列表 | ✅ 已实现（2026-08-04） |
| 批次 8 | 长任务异步化（TaskManager）：可取消 runner / 任务表 / TaskBar / 重操作异步 | ✅ 已实现（2026-08-04） |
| 批次 9 | 内置三方合并编辑器（MergeDialog 逐块选择 + wc_conflict_parse/resolve） | ✅ 已实现（2026-08-06） |
| 批次 10 | 访问历史：远程 URL / 本地路径下拉选择（history_add / history_list，JSON 持久化） | ✅ 已实现（2026-08-06） |
| 批次 11 | 认证设置页：凭据查看/清除（svn auth）+ 认证失败密码重试弹窗 | ✅ 已实现（2026-08-06） |
| 批次 12 | 并排 diff 增强：语法高亮 / 上一处下一处跳转 / 外部对比（FileMerge） | ✅ 已实现（2026-08-06） |
| 批次 13 | 冲突处理引导：update 后自动提示 + 提交禁用 | ✅ 已实现（2026-08-06） |
| 批次 14 | 远程仓库收藏：收藏当前目录 / 收藏夹 chip 切换 / 取消收藏 | ✅ 已实现（2026-08-06） |
| 批次 15 | P1 优化：分支标签管理 / 证书信任 / svn:ignore 批量 / 外部 diff 配置 | ✅ 已实现（2026-08-06） |

## 明细

### 批次 1：工作副本维护（svn resolve / cleanup / upgrade）

- [x] 冲突解决 svn resolve（conflicted 状态 → 选择 accept 策略）—— 2026-08-04 ✅
- [x] 清理 svn cleanup（工作副本管理锁修复）—— 2026-08-04 ✅
- [x] 升级 svn upgrade（旧格式工作副本）—— 2026-08-04 ✅

### 批次 2：远程写操作

- [x] 远程创建目录 svn mkdir URL -m —— 2026-08-04 ✅
- [x] 远程删除 svn delete URL -m（强确认）—— 2026-08-04 ✅
- [x] 创建分支/标签 svn copy SRC DST -m —— 2026-08-04 ✅
- [x] 远程移动/重命名 svn move SRC DST -m —— 2026-08-04 ✅
- [x] 导入本地目录 svn import —— 2026-08-04 ✅

### 批次 3：属性与锁定

- [x] 属性列表/查看 proplist / propget
- [x] 属性设置 propset（含 svn:ignore 忽略文件）
- [x] 锁定 svn lock / 解锁 svn unlock（展示拥有者、注释）

### 批次 4：本地编辑与日志

- [x] 本地移动/重命名 svn move（保留历史）
- [x] 编辑提交说明 svn propset --revprop svn:log（服务端 hook 允许时）
- [x] 远程日志过滤 UI（--search / 按日期）

> ✅ 已实现（2026-08-04）：wc_move / wc_set_log / remote_log 过滤参数（search + 日期范围，--author 由 --search 覆盖——本机 svn 1.14.5 无 --author 选项）

### 批次 5：浏览辅助

- [x] blame 逐行归属（弹层展示 行号/版本/作者/内容）
- [x] 与服务器比较 svn status -u（状态行显示 ↻ 服务器端状态 + 服务器最新 revision）
- [x] 工作副本自动刷新（15s / 30s / 60s 可选，切换时生效）
- [x] changelist 分组提交（加入 / 移除 / 按变更集提交，cwd 为工作副本根）

> ✅ 已实现（2026-08-04）：wc_blame（纯文本解析，兼容 r 前缀）/ wc_status_u / wc_changelist / wc_commit_cl（--changelist 需在 WC 根目录执行）

### 批次 6：P2 低优先

- [x] 切换分支/URL svn switch（含 --depth 稀疏目录）
- [x] 分支间合并 svn merge（-r F:T 范围或 mergeinfo 自动合并 + dry-run）
- [x] 重定位 svn relocate（服务器地址变更，位置参数语法）
- [x] 稀疏目录（--depth 控制，并入 switch/update）
- [x] 补丁创建/应用（svn diff 文本 + svn patch）
- [x] 内置三方合并编辑器 —— 2026-08-06 ✅（批次 9 已实现：MergeDialog 逐块选择 + wc_conflict_parse/resolve）

## 其他体验缺口（已并入批次 7/8）

### 批次 7：大目录虚拟列表（P2 体验，纯前端）

- [x] 抽 `VirtualList.vue` 通用组件：固定行高 + scrollTop 计算可视窗口（±buffer），只渲染可视行（不引第三方库）
- [x] RemoteView 目录列表接入虚拟列表（数千条 `svn list` 结果滚动流畅）
- [x] 日志列表接入（大日志可复用同一组件）
- [x] 验证：5000 行模拟数据滚动实测 + typecheck + build + GUI 冒烟

### 批次 8：长任务异步化（TaskManager，P2 体验，价值最高）

- [x] 后端 runner：`run_svn_cancellable(args, cwd, desc, &AtomicBool)` —— 复用现有 120s 超时循环，加取消标志 → kill 子进程
- [x] 后端 task.rs：TaskManager 全局任务表（id/desc/状态 running-done-failed-cancelled/起止时间/输出），spawn 线程执行；IPC：`task_list` / `task_cancel` + 4 个异步变体（task_checkout/task_update/task_import/task_export），同步命令保留（测试用）
- [x] 重操作接入：wc_checkout / wc_update / wc_import / remote_export 加异步变体命令，同步命令保留
- [x] 前端 TaskBar.vue 底部任务栏：进行中任务（旋转图标 + 取消按钮）+ 历史任务（失败可看详情输出）；checkout/update/import/export 按钮改走任务 API，UI 全程不卡
- [x] 集成测试 task_flow：后台 checkout 完成（Done + .svn 产出 + 结果摘要）、已结束任务不可取消、不存在的 URL 失败（Failed + 错误输出）、3000 文件大目录导出立即取消（Cancelled）
- [x] 验证：`cargo test` 32 项全绿（21 单元 + 11 集成）、typecheck、build、GUI 冒烟

### 批次 9：内置三方合并编辑器（P2 功能，工程量最大）

#### 详细开发计划（2026-08-04 制定，仅计划未实现）

##### 已实测的关键事实（本机 svn 1.14.5）
1. 冲突标记格式：`<<<<<<< .mine` / `||||||| .r3`（base 段，后缀为 revision）/ `=======` / `>>>>>>> .r4`；标记行 = 7 字符 + 空格 + 标签
2. **方向语义**：`.mine` = 本地工作副本修改、theirs 段 = 远端/服务器修改、base 段 = 共同祖先（.rNN）。与直觉相反，前端标注须按「本地 / 基准 / 远端」展示
3. 仅双方改同一区域才写标记（不同区域自动合并，无冲突文件）
4. 冲突时生成 `f.txt.mine` / `f.txt.r3` / `f.txt.r4` 备份文件，`svn resolve` 后自动清理
5. 属性冲突 / 二进制冲突**无文本标记** → `wc_conflict_parse` 返回 `has_markers=false`，前端引导走策略 resolve
6. `svn status` 冲突项 item = `conflicted`；`svn resolve --accept working` 接受当前文件内容

##### 后端（src-tauri/src/svn/）
- **parser.rs** 新增 `parse_conflict(lines)` 状态机：行首 `<<<<<<< ` 开块 → mine 段累积 → `||||||| ` 切 base → `=======` 切 theirs → `>>>>>>> ` 闭块；块外行 = 上下文行
  - 防御：`=======` / `>>>>>>>` 仅在已开块内识别（markdown 分隔线不误判）；未闭合块返回明确错误
  - **字节级解析**：同时保存 `Vec<Vec<u8>>` 原始字节行，写回按字节拼接（不经 unescape，非 UTF-8 文件不损坏）
  - 检测原行尾（`\r\n` vs `\n`），组装时还原
- **models.rs** 新增 `ConflictBlock { mine: Vec<String>, base: Vec<String>, theirs: Vec<String> }`、`ConflictInfo { blocks, has_markers, line_ending }`
- **commands.rs** 新增：
  - `wc_conflict_parse(path)` → `ConflictInfo`（展示文本经 UTF-8 容错替换）
  - `wc_conflict_resolve(path, choices: Vec<String>)`：写锁 → 重读文件重解析 → 块数校验（不符报「文件已变化，请重新打开」）→ 按选择组装（mine→本地行 / theirs→远端行 / both→本地+远端 / none→空）→ 原行尾写回 → `svn resolve --accept working path`
- **lib.rs** 注册 `wc_conflict_parse` / `wc_conflict_resolve`

##### 前端
- **types.ts**：`ConflictBlock`、`ConflictInfo`、`ConflictChoice = "mine"|"theirs"|"both"|"none"`
- **api.ts**：`wcConflictParse(path)`、`wcConflictResolve(path, choices)`
- **MergeDialog.vue**（新组件）：路径标题 + 「上一处/下一处」块导航（当前块高亮 + 序号/总数）；三栏并排（本地 mine / 基准 base / 远端 theirs）同步滚动；底部当前块 4 选按钮（取本地/取远端/都留/都删）+ 实时合并预览；未处理块标「待处理」，全部处理完「保存并解决」才可用 → 调 resolve → 关闭 → 刷新；取消不写回
- **WcView.vue**：conflicted 行操作区加「三方合并」按钮（补充现有 resolve 策略下拉）

##### 集成测试（tests/svn_flow.rs 新增 `merge3_flow`）
1. 双 wc 改同一行 → update → conflicted
2. parse 断言：1 块、mine=本地修改、base=原行、theirs=远端修改（方向断言）
3. 场景矩阵：each choice（mine/theirs/both/none）→ resolve → status 清除 + 内容断言 + 备份文件已清理
4. 多块：改两处 → parse 2 块 → 分别选不同 → 内容正确
5. 无标记场景：`has_markers=false` 路径
6. parser 单测：markdown `=======` 不误判、未闭合块报错、CRLF 保留

##### 验收标准
cargo test 全绿（新增 merge3_flow + parser 单测）→ npm run typecheck → npm run build → GUI 冒烟 → 手动双 wc 冲突逐块合并提交闭环

##### 风险与对策
| 风险 | 对策 |
|---|---|
| 非 UTF-8 文件损坏 | 字节级解析 + 原始字节行组装 |
| CRLF 行尾破坏 | 检测原行尾并还原 |
| 标记后缀版本差异（.working/.rNN） | 只认前缀 7 字符，忽略后缀 |
| 弹窗期间文件被外部修改 | resolve 时重读并校验块数 |
| markdown `=======` 误判 | 分隔符只在块内识别 |
| 属性/二进制冲突无标记 | `has_markers=false` → 前端引导走策略 resolve |

##### 实现清单（实现阶段逐项勾选）
- [x] 后端 `wc_conflict_parse(path)`：解析冲突标记 `<<<<<<< .mine` / `=======` / `>>>>>>> .theirs`（兼容 `||||||| .base`），返回 `ConflictBlock[]`（mine/base/theirs 行 + 未定态）—— 2026-08-06 ✅
- [x] 后端 `wc_resolve_custom(path, blocks)`：按逐块选择（mine/theirs/两段都留）组装结果写回 + `svn resolve --accept working`—— 2026-08-06 ✅
- [x] 前端 MergeDialog.vue：冲突块导航（上/下一处）+ 每块单选 mine / theirs / 都留 + 合并结果实时预览 —— 2026-08-06 ✅
- [x] WcView 冲突路径增加「三方合并」按钮（补充现有 resolve 策略下拉）—— 2026-08-06 ✅
- [x] 集成测试：双 wc 改同文件 → 冲突 → 逐块选择 → 写回 → status 清除 + 内容正确 —— 2026-08-06 ✅

### 批次 10：访问历史（远程 URL / 本地路径下拉）

- [x] 后端 `history.rs`：`history_add` / `history_list`，JSON 持久化 `$HOME/.config/svn-desktop-tool/history.json`；同 kind 去重、最近使用优先、上限 50 条 —— 2026-08-06 ✅
- [x] 前端 RemoteView 仓库 URL 输入框 + WcView 工作副本路径输入框：datalist 下拉展示历史，打开成功才记录 —— 2026-08-06 ✅
- [x] 验证：cargo test 35 项全绿 + typecheck + build + GUI 冒烟；顺手修复 local_move_and_log_flow 硬编码日期（动态取系统日期） —— 2026-08-06 ✅

### 批次 11：认证设置页（建议优先级 1：企业 HTTPS 刚需）

- [x] 后端 `svn_auth_list()`：解析 `svn auth` 输出（分隔线分块、svn.simple / svn.ssl.server、还原 `{U+XXXX}` 转义）→ `AuthCred[]`（kind/realm/username/fingerprint/passwordCache）—— 2026-08-06 ✅
- [x] 后端 `svn_auth_remove(patterns)`：`svn auth --remove PATTERN...`（"*" 全清，E200009 无匹配忽略）—— 2026-08-06 ✅
- [x] 后端认证重试：runner 支持 stdin 密码，新增 `remote_open_auth(url, username, password)`（`--non-interactive --username U --password-from-stdin`，密码仅进 stdin，不进参数/日志/URL）—— 2026-08-06 ✅
- [x] 前端设置面板加「认证」区块：凭据列表 + 单条清除 / 全部清除 / 刷新—— 2026-08-06 ✅
- [x] 前端 remote_open 认证失败（E170001）→ 弹用户名密码框 → 带凭据重试—— 2026-08-06 ✅
- [x] 证书信任三候选机制（ssl-authority-files / 命令行一次性接受 / 隔离 HOME）标注「待 HTTPS fixture 验证后定案」，本次不做—— 2026-08-06 ✅（待办）

### 批次 12：并排 diff 增强（建议优先级 2：日常高频）

- [x] `npm install @codemirror/language-data`，DiffView 按扩展名自动匹配语言语法高亮（a/b.extensions）—— 2026-08-06 ✅
- [x] diff 工具栏「上一处 / 下一处」跳转（mv.b 跳转 + N/M 标签）—— 2026-08-06 ✅
- [x] 双击 diff 区或按钮打开外部工具：后端 `wc_diff_external(path)` 导出 BASE/工作区临时文件 → `opendiff`（FileMerge）对比—— 2026-08-06 ✅

### 批次 13：冲突处理引导（建议优先级 3：新手友好）

- [x] WcView update 完成后检测 conflicted → 自动弹引导条（冲突路径清单 + 「三方合并」/「策略解决」入口）—— 2026-08-06 ✅
- [x] 存在 conflicted 时提交按钮禁用 + 提示先解决冲突—— 2026-08-06 ✅

### 批次 14：远程仓库收藏

- [x] 后端 `favorites.rs`：`fav_add` / `fav_list` / `fav_remove`，JSON 持久化 `$HOME/.config/svn-desktop-tool/favorites.json`；按 URL 去重（更新名称移到最前）、上限 100；3 个单测 —— 2026-08-06 ✅
- [x] 前端 RemoteView：「☆ 收藏 / ★ 已收藏」按钮（收藏当前浏览目录，已收藏时点击取消）；收藏夹 chip 栏（点击切换过去、× 删除、当前项高亮）—— 2026-08-06 ✅
- [x] 验证：cargo test 53 项全绿（40 单元含 3 收藏测试 + 13 集成）+ typecheck + build + GUI 冒烟 —— 2026-08-06 ✅
