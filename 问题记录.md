# 问题记录

记录开发过程中遇到的问题：编号、描述、影响、状态、解决方案。

## 登记规则

- 状态：`待处理` / `处理中` / `已解决` / `已规避` / `关闭`
- 严重度：`高` / `中` / `低`

## 问题列表

### 001 ｜ svn list --xml 与 -v 不兼容

- 日期：2026-08-03
- 严重度：高（远程目录列表完全不可用）
- 状态：已解决
- 描述：`svn list --xml -v URL` 报 `E205000: --verbose is not valid in --xml mode`。svn 的 `--xml` 模式自带全部字段（name/size/commit revision/author/date），`-v` 非法。
- 影响：remote_open 预检与 remote_list 全部失败。
- 解决：去掉 `-v`，仅用 `--xml`；实测 XML 结构与 parser 兼容。

### 002 ｜ LC_ALL=C 下中文提交说明报 E000022

- 日期：2026-08-03
- 严重度：高（中文用户提交必现）
- 状态：已解决
- 描述：子进程统一 `LC_ALL=C` 后，svn 把 native encoding 判定为 ASCII，`-F` 消息文件含中文时报 `E000022: Can't convert string from native encoding to 'UTF-8'`。
- 影响：所有含非 ASCII 的提交说明失败。
- 解决：commit 显式加 `--encoding utf-8` 声明消息编码，与输出 locale 策略解耦。集成测试用中文提交说明验证通过。

### 003 ｜ warning 错误码 W 前缀不参与分类

- 日期：2026-08-03
- 严重度：中（错误分类误报）
- 状态：已解决
- 描述：svn 对部分目标缺失等输出 `W160013`（warning 前缀），原分类只匹配 `E160013`，导致 W160013+E200009 组合被误分类为 wc-error 而非 not-found。
- 解决：错误码提取改为 E/W 双前缀；分类匹配时 W 码按对应 E 码处理。补充单元测试覆盖。

### 004 ｜ svn import 导入目录内容而非创建同名子目录

- 日期：2026-08-03
- 严重度：低（仅测试环境）
- 状态：已解决
- 描述：`svn import work file:///repo` 将 work 的**内容**导入仓库根，不会创建 `/work` 子目录（与 TortoiseSVN 导入对话框行为不同）。测试初始 URL 指向 `/work` 导致 W170000 not found。
- 解决：测试中 trunk 指向仓库根；已在测试代码注释说明该语义。

### 005 ｜ wc_diff 将 current_dir 设为文件路径

- 日期：2026-08-03
- 严重度：中（本地 diff 不可用）
- 状态：已解决
- 描述：`wc_diff` 把 `cwd` 传给文件完整路径，`current_dir` 要求目录，报 `Not a directory (os error 20)`。
- 解决：cwd 改为文件所在父目录（parent），绝对路径传入保持语义不变。

### 006 ｜ vue-tsc 对 ref\<Set\<string\>\> 展开的类型推断失败

- 日期：2026-08-03
- 严重度：低（类型检查阻塞）
- 状态：已解决
- 描述：`[...checked.value]`（Set 展开）在 vue-tsc 下报 `Property 'value' does not exist on type 'Set<string> & Omit<...>'`，Vite build 却正常，属于 vue-tsc 与泛型 ref 的推断问题。
- 解决：勾选集改用 `ref<string[]>`，逻辑等价（includes/splice）。

### 007 ｜ svn 自动 mime 检测把无扩展名+非 ASCII 文件判为二进制

- 日期：2026-08-03
- 严重度：中（diff 与预览行为受影响）
- 状态：已解决
- 描述：`README.md` 内容含中文时，svn import 自动设置 `svn:mime-type = application/octet-stream`，此后 diff 输出 `Cannot display: file marked as a binary type.`（与 "(Binary files differ)" 文案不同）。
- 解决：① `diff_result` 的二进制检测补充 "Cannot display" 文案；② 测试用例把中文内容放到 `.txt` 文件，无扩展名文件用 ASCII 内容（与真实用户行为一致，属于 svn 既定行为而非缺陷）。

### 008 ｜ Tauri dev 模式 WebKit WebsiteData 沙盒警告

- 日期：2026-08-03
- 严重度：低（仅日志噪音）
- 状态：已规避
- 描述：启动日志出现 `could not create directory ".../WebKit/svn-desktop-tool/WebsiteData/..."`（NSCocoaErrorDomain Code=513）。dev 模式下应用签名/沙盒配置不完整导致 WebKit 网站数据目录不可写。
- 影响：不影响功能；localStorage 等持久化在 dev 下可能不可靠（与 Tortoise_git_mac 问题同源）。
- 处理：计划已注明“发布签名后解决”；MVP 无本地持久化依赖，暂不处理。

### 009 ｜ 大文件 diff 主线程卡顿

- 日期：2026-08-04
- 严重度：中（大文件 diff 时 UI 卡死）
- 状态：已解决
- 描述：并排 diff 用 @codemirror/merge 内置算法在 WKWebView（JSCore 比 V8 慢 20–40%）计算大文件（数百 KB 以上）差异时占用主线程，滚动与点击明显卡顿。
- 影响：大文件 diff 体验差；merge 内置 diff 对超长文本还有 scanLimit（默认 500 行）精度回退。
- 解决：新增 Rust 命令 diff_chunks（similar crate 行级 diff，返回字符位置变更块，与 merge Chunk 语义一致）；前端两侧文本总字符 > 400K 时改调 Rust 计算，MergeView 以 chunks 选项只渲染。新增 6 个单测覆盖替换/插入/删除/追加/相等场景。

### 010 ｜ 中文 URL 打开远程仓库无反应（双重根因）

- 日期：2026-08-04
- 严重度：高（中文仓库 URL 完全无法使用）
- 状态：已解决
- 描述：输入 `https://<SVN服务器>/svn/<项目名>…` 点“打开”后界面无任何反应。两个根因叠加：
  1. runner 子进程设 `LC_ALL=C`，svn 按 LC_CTYPE 解释命令行参数，非 ASCII 参数（中文 URL/路径）报 E000022 转换失败或双重编码（“轨”的 UTF-8 字节 E8 BD A8 被当 latin-1 再编码成 %C3%A8%C2%BD%C2%A8），服务器按错误路径返回 forbidden；
  2. RemoteView/WcView 模板从未渲染组件内 `err` ref（App.vue 底部错误条只显示 App 自己的 err），错误完全静默。
- 影响：所有含中文的仓库 URL 与工作副本路径都无法打开。
- 解决：runner 改 `LC_ALL=C`/`LANG=C` → 删除 LC_ALL/LANG + `LC_MESSAGES=C`（错误消息稳定英文）+ `LC_CTYPE=en_US.UTF-8`（参数正确 UTF-8 编码）；两个页面组件 openbar 下新增 `v-if="err"` 错误提示块。实测用户 URL 可正常 svn info/list（Revision 38766，目录 0_日常管理工作/、1_管理类/、2_开发库/、3_受控库/），无需认证。
